---
import { NAVIGATION_LINKS, SOCIAL_LINKS, OTHER_LINKS } from "~/lib/config";
import Icon from "../bases/icon.astro";
---

<footer class="footer border-t border-base-content py-6">
  <div class="container">
    <div class="flex flex-col gap-4">
      <img class="lg:hidden" src="/logo.png"/ style="width: 2rem;" alt="Logotipo O Abrangente">
      <aside class="flex items-center gap-2">
        <img class="hidden lg:flex" src="/logo.png"/ style="width: 2rem;" alt="Logotipo O Abrangente">
        <p>
          <strong class="text-lg">O Abrangente</strong>
          <br />
          Um portal de notícias desenvolvido com uma base de conhecimento ampliada, dedicado à cobertura e análise de eventos atuais.
          <!--A news website built with an augmented knowledge base that reports and analyzes current events.-->
        </p>
      </aside>
      <nav
        class="flex flex-col gap-4 lg:flex-row lg:flex-wrap text-base lg:text-sm font-semibold"
      >
        <a href="/" class="link link-hover">Início</a>
        <a href="/articles" class="link link-hover">Notícias</a>
        {
          NAVIGATION_LINKS.map(({ href, text, target }) => (
            <a href={href} class="link link-hover" target={target ?? "_self"}>
              {text}
            </a>
          ))
        }
      </nav>
    </div>
    <div class="divider w-full h-1"></div>
    <div class="flex flex-col gap-4">
      <nav>
        <div class="flex gap-4">
          <p class="font-bold">Redes Sociais:</p>
          <ul class="flex flex-wrap gap-4">
            {
              SOCIAL_LINKS.map(({ href, icon, text, target }) => (
                <li>
                  <a
                    href={href}
                    class="link link-hover"
                    target={target ?? "_blank"}
                    title={text}
                    aria-label={text}
                  >
                    <Icon icon={icon} />
                  </a>
                </li>
              ))
            }
          </ul>
        </div>
      </nav>
      <nav class="flex flex-wrap gap-4 text-xs mt-2">
        {
          OTHER_LINKS.map(({ href, text, target }) => (
            <a href={href} class="link link-hover" target={target ?? "_self"}>
              {text}
            </a>
          ))
        }
      </nav>
    </div>
  </div>
</footer>
